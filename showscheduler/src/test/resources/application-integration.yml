show-scheduler:
  reservation-buffer: PT1M

clients:
  cinemas:
    url: http://localhost:${wiremock.server.port}
    read-timeout: 1000
  movies:
    url: http://localhost:${wiremock.server.port}
    read-timeout: 1000