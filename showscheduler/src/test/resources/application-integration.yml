show-scheduler:
  reservation-buffer: PT1M

clients:
  cinemas:
    url: http://localhost:${wiremock.server.port}
    readTimeout: 1000
  movies:
    url: http://localhost:${wiremock.server.port}
    readTimeout: 1000

resilience4j.timelimiter:
  configs:
    default:
      timeoutDuration: 3s